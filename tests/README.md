# Detection Rule Testing

This folder contains test cases used to validate every detection rule.

Every detection rule must have:

- Test case
- Expected result
- Actual result
- Status (Pass / Fail)

The detector should never include a rule that has not been tested.